From 50e0e0689f012f8be173e6e3d21760565ec4fd9a Mon Sep 17 00:00:00 2001
From: Tim Harsch <tharsch@yarcdata.com>
Date: Fri, 29 Jul 2016 19:59:32 -0500
Subject: [PATCH 2/4] add to usage. no timings for extended format

---
 libexec/bats           | 1 +
 libexec/bats-exec-test | 2 +-
 2 files changed, 2 insertions(+), 1 deletion(-)

diff --git a/libexec/bats b/libexec/bats
index 85cdf61..cdd378f 100755
--- a/libexec/bats
+++ b/libexec/bats
@@ -20,6 +20,7 @@ help() {
   echo "  -h, --help     Display this help message"
   echo "  -p, --pretty   Show results in pretty format (default for terminals)"
   echo "  -t, --tap      Show results in TAP format"
+  echo "  -d             Durations for TAP output with YAML format"
   echo "  -v, --version  Display the version number"
   echo
   echo "  For more information, see https://github.com/sstephenson/bats"
diff --git a/libexec/bats-exec-test b/libexec/bats-exec-test
index 6839b54..cec2be5 100755
--- a/libexec/bats-exec-test
+++ b/libexec/bats-exec-test
@@ -271,7 +271,7 @@ bats_exit_trap() {
     status=0
   fi
 
-  if [[ -n $BATS_RECORD_DURATIONS && -n $BATS_TEST_DURATION ]]; then
+  if [[ -z $BATS_EXTENDED_SYNTAX && -n $BATS_RECORD_DURATIONS && -n $BATS_TEST_DURATION ]]; then
     echo "  ---" >&3
     echo "    duration_ms: $BATS_TEST_DURATION" >&3
     echo "  ..." >&3
-- 
2.10.2

